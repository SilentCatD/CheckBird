import 'package:flutter/material.dart';

class GroupTopicTab extends StatelessWidget {
  const GroupTopicTab({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text("This is chat topic tab"),
    );
  }
}
